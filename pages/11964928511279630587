<h1>
Java: Comparing two lists - Stack Overflow
<\h1>
<p>I have two lists. 
For example: </p>
<pre><code>A = {21, 41, 96, 02}
B = {21, 96, 32, 952, 1835}
</code></pre>
<p>What I want is this as a result: <code>R = {32, 952, 1835}</code></p>
<p>Like this: 
<img alt="Example Picture" src="http://i.stack.imgur.com/CJj8U.png"/></p>
<p>After this I want to add the result <code>R</code> to <code>A</code>:</p>
<pre><code>A = {21, 41, 96, 02, 32, 952, 1835}
</code></pre>
<p>It's simple :)</p>
<pre><code>List&lt;Integer&gt; a = new ArrayList&lt;&gt;(Arrays.asList(21, 41, 96, 02));
List&lt;Integer&gt; b = new ArrayList&lt;&gt;(Arrays.asList(21, 96, 32, 952, 1835));

b.removeAll(a)

// now list b contains (32, 952, 1835)

a.addAll(b);

// now list a contains (21, 41, 96, 02, 32, 952, 1835)
</code></pre>
<p>So, you want the set reunion of those two collections. That's what <a href="http://docs.oracle.com/javase/7/docs/api/java/util/HashSet.html" rel="nofollow">sets</a> are for.</p>
<pre><code>HashSet&lt;Integer&gt; set = new HashSet&lt;Integer&gt;();
set.addAll(a);
set.addAll(b);
</code></pre>